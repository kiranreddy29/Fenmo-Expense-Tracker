{
  "name": "fenmo-expense-tracker-backend",
  "version": "1.0.0",
  "description": "Backend API for Fenmo Expense Tracker",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "mkdir -p data && node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^11.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
